<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>

<body>
    <%- include('../partials/header') %>
    <main>
        <div class="container">
            <% users.forEach(user => { %>
            <%- include('users', { user }) %>
            <% }) %>
        </div>
    </main>
</body>

</html>